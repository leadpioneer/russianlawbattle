# LLM / оркестрация агентов
openai>=1.60.0
langgraph>=0.2.60
langchain-core>=0.3.29

# CLI и красивый вывод
typer>=0.12.5
rich>=13.7.0

# Конфигурация
PyYAML>=6.0.1
python-dotenv>=1.0.1

# Чтение документов дела
pypdf>=4.3.0
python-docx>=1.1.2

# Веб-API (этап 2): FastAPI + WebSocket-стриминг событий
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.12

# MCP: актуальные нормы права через pravo.gov.ru (этап 2, шаг 4)
mcp>=1.2.0
# pravo-mcp не опубликован на PyPI; wheel собран из исходников
# (github.com/AlsKozlov/ru-legal, mcps/pravo) и положен в wheels/ для offline-установки.
# Обновить wheel: pip wheel "git+https://github.com/AlsKozlov/ru-legal.git#subdirectory=mcps/pravo" -w wheels --no-deps
./wheels/pravo_mcp-0.1.0-py3-none-any.whl

# Правовой research layer (этап 3): HTTP к pravo.gov.ru и КонсультантПлюс,
# markitdown — HTML/PDF→Markdown для получения текстов статей НПА
httpx>=0.27.0
markitdown[pdf]>=0.1.0

# Тесты (этап 3): unit/integration-тесты новой бизнес-логики
pytest>=8.0.0
pytest-asyncio>=0.24.0

